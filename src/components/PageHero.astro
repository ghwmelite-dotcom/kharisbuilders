---
interface Props {
  image: string;
  height?: string;
  align?: 'center' | 'bottom';
}
const { image, height = 'h-[460px] md:h-[600px]', align = 'center' } = Astro.props;
const alignWrap = align === 'bottom' ? 'items-end' : 'items-center justify-center';
const contentClass =
  align === 'bottom'
    ? 'w-full max-w-[var(--container-max)] mx-auto px-margin-mobile md:px-margin-desktop pb-14'
    : 'text-center px-6 max-w-4xl mx-auto';
---
<header class={`relative ${height} flex ${alignWrap} overflow-hidden bg-primary`}>
  <img src={image} alt="" class="kenburns absolute inset-0 w-full h-full object-cover opacity-[0.88]" />
  <!-- theme-toned legibility gradient: lets the photo show, darkens top + bottom for the title -->
  <div
    class="absolute inset-0"
    style="background: linear-gradient(180deg, color-mix(in srgb, var(--kb-primary-container) 44%, transparent) 0%, color-mix(in srgb, var(--kb-primary-container) 8%, transparent) 48%, color-mix(in srgb, var(--kb-primary-container) 78%, transparent) 100%);"
  >
  </div>
  <!-- brand-color glows (subtle) -->
  <div class="hero-glows absolute inset-0 pointer-events-none opacity-[0.45]">
    <span class="glow glow-orange"></span>
    <span class="glow glow-blue"></span>
    <span class="glow glow-purple"></span>
  </div>
  <!-- focused scrim behind text (keeps gold eyebrows + white titles legible over the brighter photo) -->
  <div
    class="absolute inset-0 pointer-events-none"
    style="background: radial-gradient(60% 58% at 50% 50%, color-mix(in srgb, var(--kb-primary-container) 56%, transparent) 0%, color-mix(in srgb, var(--kb-primary-container) 26%, transparent) 50%, transparent 80%);"
  >
  </div>
  <div class={`relative z-10 ${contentClass}`} data-reveal>
    <slot />
  </div>
</header>
